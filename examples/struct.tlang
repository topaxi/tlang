struct Point {
    x: isize,
    y: isize,
}

fn Point::new(x: isize, y: isize) -> Point {
    Point {
        x: x,
        y: y,
    }
}

// Point.get_coordinates(self) -> isize[]
fn Point.get_coordinates(self) {
    [self.x, self.y]
}

let p = Point::new(5, 10);

p.get_coordinates()
|> log();
