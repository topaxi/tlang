struct Point {
    x: i64,
    y: i64,
}

fn Point::new(x: i64, y: i64) -> Point {
    Point {
        x: x,
        y: y,
    }
}

// Point.get_coordinates(self) -> i64[]
fn Point.get_coordinates(self) {
    [self.x, self.y]
}

let p = Point::new(5, 10);

p.get_coordinates()
|> log();
