protocol Area {
  fn area(self)
}

struct Rect { width: isize, height: isize }

impl Area for Rect {
  fn area(r) { r.width * r.height }
}

let r = Rect { width: 5, height: 3 };
Area::area(r) |> log();
