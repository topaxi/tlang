struct A {}

fn A::new() {
    A {}
}

fn method(_self: A) {
    "called with just self"
}
fn method(_self: A, _a: i64) {
    "called with a single argument"
}

let a = A::new();

method(a)
|> log();

method(a, 1)
|> log();
