struct A {}

fn A::new() {
    A {}
}

fn A.method(_self) {
    "called with just self"
}
fn A.method(_self, _a) {
    "called with a single argument"
}

let a = A::new();

a.method()
|> log();

a.method(1)
|> log();
