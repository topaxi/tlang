// Compute the sum of each row using nested for-loops
// (inner accumulator, outer simple iteration)
for row in [[1, 2, 3], [4, 5, 6], [7, 8, 9]] {
  let row_sum = for cell in row; with s = 0 {
    s + cell
  };
  log(row_sum);
};
