// Test protocol with structs
protocol Area {
  fn area(self)
}

struct Rect { width: isize, height: isize }

impl Area for Rect {
  fn area(self) { self.width * self.height }
}

let r = Rect { width: 5, height: 3 };
Area::area(r) |> log();
