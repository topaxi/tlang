struct Vector {
    x: i64,
    y: i64,
}

// Vector::new(i64, i64) -> Vector
fn Vector::new(x: i64, y: i64) -> Vector {
    Vector { x, y }
}

// Vector.add(self, Vector) -> Vector
fn Vector.add(self, other: Vector) -> Vector {
    Vector::new(self.x + other.x, self.y + other.y)
}

// Vector.scale(self, i64) -> Vector
fn Vector.scale(self, scalar: i64) -> Vector {
    Vector::new(self.x * scalar, self.y * scalar)
}

fn int_sqrt(n: i64, guess: i64) -> i64 {
    if guess * guess >= n {
        guess
    } else {
        rec int_sqrt(n, guess + 1)
    }
}

// Vector.magnitude(self) -> i64
fn Vector.magnitude(self) -> i64 {
    int_sqrt(self.x * self.x + self.y * self.y, 0)
}

// Example usage
let v1 = Vector::new(3, 4);

// Vector addition
Vector::new(1, 2)
|> v1.add()
|> log();

// Scaling a vector
v1.scale(2)
|> log();

// Magnitude of a vector
v1.magnitude()
|> log();
