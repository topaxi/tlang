# tlang_macros

Procedural macros for tlang development.

## Features

*   `#[native_fn]`: Attribute macro to simplify defining native functions for the tlang standard library/runtime. Handles boilerplate for argument parsing and return value wrapping.
