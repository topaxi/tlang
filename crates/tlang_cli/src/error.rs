// This module is kept for backward-compatible `use crate::error::*` paths.
// The ParserError bridge has been removed; the compile pipeline now uses
// `Vec<tlang_diagnostics::Diagnostic>` directly.
