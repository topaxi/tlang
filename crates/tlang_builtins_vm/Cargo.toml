[package]
name = "tlang_builtins_vm"
version = "0.1.0"
edition.workspace = true
description = "Semantic symbol definitions for the tlang interpreter (VM) standard library"

[dependencies]
tlang_defs = { path = "../tlang_defs" }

[lints]
workspace = true
